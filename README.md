# Decimal Placement Visualizer

A small D3 web app for teaching multiplication and division by powers of ten.

Open `index.html` directly, or run a local server:

```sh
python3 -m http.server 8000
```

Then visit `http://127.0.0.1:8000`.
